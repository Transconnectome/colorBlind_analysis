% Appendix A — Alternative model comparison
% Reconciled 2026-06-23 with the main-text composite criterion: the retinal-family
% (R+C) model is the 1-DOF g-only form (delta_theta = (2-g) delta_theta_Mach;
% Methods sec:methods:candidates), scored on the same held-out composite as the
% 2-component model. Historical LOCO-rho-argmax fits and the 2-DOF (Delta-lambda, g)
% decomposition have been removed; only the criterion-independent structural
% arguments are retained.

\section*{Appendix A: Alternative Model Comparison}
\label{app:altmodels}

The main text adopts a 2-component cortical model (Section~\ref{sec:results:twocomp}). Here we compare it against the retinal-family alternative on the same held-out composite criterion (\S\ref{sec:methods:selection}), and give the structural reasons the retinal-family class is inadequate for the stimulus-space filter task.

\subsection*{A.1 Retinal-plus-cortical (R+C) fit on the composite criterion}

The retinal-plus-cortical model applies the subtype Machado cone-shift $\delta\theta_{\rm Mach}$ \citealp{machado2009} scaled by a single cortical-gain parameter $g$, where $\delta\theta_{\rm RC} = (2 - g)\,\delta\theta_{\rm Mach}$, so that $g = 1$ is retinal-only and $g = 2$ makes the residual distortion vanish by construction. The form follows the finding that post-receptoral gain amplifies the attenuated retinal signal until the group difference is abolished by V2v and V3v \citealp{tregillus2021}, whose gains scale response amplitude rather than a hue displacement. Scored on the same held-out composite as the 2-component model, R+C failed for both participants: for the deutan participant every resample saturated the gain boundary ($g \to 3.0$, $100\%$), so the fit was rejected as misspecified; for the protan participant the best fit ($g = 2.95$) generalized worse than the adopted 2-component model ($\overline{L}_{\rm test} = -0.86$ vs.\ $-1.54$). Full fits are in Supplementary Table~\ref{tab:modelfits} and Section~\ref{sec:results:rc_insufficient}. A gain $g > 2$ implies cortical overshoot past the undistorted hue, inconsistent with the participants' confirmed residual CVD, so the saturated estimate is a model failure rather than a parameter value.

\subsection*{A.2 Inverse-mapping (pre-image) behavior}

The angular form of the 2-component model is bijective by construction: a stimulus-space pre-image $\theta + \delta\theta$ exists for any target hue. The filter reported in Section~\ref{sec:results:filter} is that pre-image, evaluated at the eight stimulus hues.

\subsection*{A.3 Why the cortical gain cannot recover the distortion}

The cortical gain $g$ rescales the retinal shift uniformly, so for every value of $g$ the R+C family displaces hues along the fixed confusion axis alone. It cannot generate the 2-component model's S-cone-axis term $\beta_s \cos(\theta - 90^\circ)$, a direction that lies $60^\circ$ from the deutan confusion axis and $74^\circ$ from the protan one. The observed interpolation deficit concentrates at the S-cone intermediate hues (Section~\ref{sec:results:loco}), a displacement pattern this family cannot represent. The restriction is a property of the model class and holds across fitting criteria.
