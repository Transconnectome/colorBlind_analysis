% Results Section — v4 (2026-05-14)
% Changes from v3:
%   - Figure numbering: Fig 1 (paradigm), Fig 2 (forward encoder),
%                       Fig 3 (LORO/LOCO), Fig 4 (geometry),
%                       Fig 5 (2-component), Fig 6 (filter)
%   - Fig 1 (paradigm) moved to Methods to render before Fig 2 (forward encoder).
%   - Primary model: 2-component throughout; Machado and R+C comparisons moved to Appendix A
%   - LOCO metric updated: adjacent accuracy (adj_acc, 0–1, higher = better)
%     replacing circular correlation rho
%   - Fig 4 (SRM geometry/ΔRDM) added as separate figure
%   - Fig 1 is a PowerPoint-generated schematic: placeholder box below
%   - All captions from FIGURE_CAPTIONS.md (2026-05-11 revision)
% 2026-05-14 update (Option C adoption):
%   - Loss changed HYBRID → Option C (neural-primary 4-term + heavy Tikhonov, Methods Eq. 2).
%     Sub-08 argmin (16,+40) → (40,+26); sub-09 (12,-30) → (12,-28).
%   - Brettel directional-consistency text removed per 2026-05-14 revalidation.
%   - Sub-08 filter-direction description rewritten for new argmin.
%   - Model-class ceiling caveat added.
%   - HYBRID backup at results_v4_HYBRID_backup.tex.

\section{Results}
\label{sec:results}

% ──────────────────────────────────────────────────────────────────────────────
\subsection{Color discrimination is preserved across all ROIs in both CVD cases}
\label{sec:results:loro}
% ──────────────────────────────────────────────────────────────────────────────

A corrective stimulus-space filter is valid only if the visual cortex of a CVD individual retains categorical discrimination among the target hues.
We verified this precondition using leave-one-run-out (LORO) cross-validation of the forward encoding model in SRM-aligned space.

Both CVD participants exceeded the 0.125 exact-accuracy chance level at every ROI (Figure~\ref{fig:loco}A).
A cross-subject generalization test of the same forward encoding model (HC-to-HC vs.\ HC-to-CVD pair-wise cross-decoding, all ROIs pooled) showed no HC--CVD difference (Mann--Whitney $U$ test, 21 HC-to-HC vs.\ 14 HC-to-CVD pairs, $p = 0.668$, rank-biserial effect size and exact test statistic in Supplementary~\ref{app:effect_sizes}).
The same null held at the within-ROI level at hV4 (Crawford \& Howell $t$-test, both CVD subjects within the HC range, $p = 0.142$).
The 8 scanner stimuli are therefore individually distinguishable at the cortical level in both CVD participants, in line with prior work on CVD above-threshold identification \cite{bosten2019, boehm2014}.
The representational substrate on which a corrective filter could act is present.

% ──────────────────────────────────────────────────────────────────────────────
\subsection{Color interpolation is impaired in both CVD cases}
\label{sec:results:loco}
% ──────────────────────────────────────────────────────────────────────────────

We next localized where continuous hue interpolation fails, which defines the functional target of the corrective transform, using leave-one-color-out (LOCO) decoding of the forward encoding model.
Among the four retinotopic ROIs, only hV4 supported above-chance hue interpolation in HC (adjacent accuracy $0.47 \pm 0.05$, mean $\pm$ SEM, $p = 0.044$ under $8! = 40{,}320$ exact label permutations).
V1--V3 did not exceed chance despite preserving cross-run discrimination.
This confirms hV4 as the primary interpolation gate, replicating \citeA{brouwer2009}.

Both CVD participants fell at or below the adjacent-accuracy chance level at hV4 (Figure~\ref{fig:loco}B).
Sub-08 reached adjacent accuracy $0.25$ (Crawford \& Howell $t = -1.58$, $p = 0.082$, n.s., $d_{cc} = -1.71$).
Sub-09 reached $0.13$ ($t = -2.48$, $p = 0.024$, $d_{cc} = -2.68$).
The impairment was not uniform across hues.
Both participants showed near-zero adjacent accuracy at hV4 for the S-cone intermediate colors blue, purple, and magenta (adjacent accuracy $= 0$ for both participants at all three hues; Figure~\ref{fig:loco}C).
Per-hue Crawford \& Howell tests (one-tailed) reached significance only for blue (both participants $d = 2.20$, $p = 0.042$); purple ($d = 1.02$, $p = 0.19$) and magenta ($d = 1.89$, $p = 0.064$) did not survive the single-case test against the $n = 7$ HC distribution.
These per-hue accuracies form each participant's hue vulnerability profile $\mathbf{v} \in [0,1]^8$.

% ── Figure 3 ─────────────────────────────────────────────────────────────────
\begin{figure*}[tb]
  \centering
  \includegraphics[width=\textwidth]{fig2_loro_loco}
  \caption{\textbf{Color discrimination is preserved but interpolation is selectively impaired at hV4 in CVD.} \textbf{(A)} LORO discrimination accuracy (forward encoding model, 8-class, SRM-aligned), V1--hV4.
  HC mean $\pm$ SEM ($n = 7$, gray); sub-08 (orange square), sub-09 (teal triangle).
  Chance = $1/8$.
  \textbf{(B)} LOCO adjacent accuracy ($\pm 1$ hue step; chance $= 3/8$).
  \emph{$*$}: sub-09 below HC at hV4 (Crawford \& Howell $t = -2.48$, $p = 0.024$; exploratory single-case finding); sub-08 $t = -1.58$, $p = 0.082$, n.s.
  \textbf{(C)} Per-hue adjacent accuracy at hV4, HC bars $\pm$ SEM.
  Per-hue Crawford \& Howell tests (one-tailed) were significant only for blue (both participants $d = 2.20$, $p = 0.042$); purple ($d = 1.02$, $p = 0.19$) and magenta ($d = 1.89$, $p = 0.064$) were n.s.
  Joint pattern in (A) and (B--C) defines the filter precondition and target, respectively.}
  \label{fig:loco}
\end{figure*}

% ──────────────────────────────────────────────────────────────────────────────
\subsection{Color geometry is distorted at a distinct ROI in each CVD case}
\label{sec:results:geometry}
% ──────────────────────────────────────────────────────────────────────────────

To characterize the geometric basis of the LOCO impairment, we compared the pairwise representational structure of CVD and HC participants in a shared SRM-aligned space.
For each CVD participant we computed disparity and the per-pair RDM deviation from the HC mean ($\Delta\text{RDM} = \text{RDM}_\text{CVD} - \overline{\text{RDM}}_\text{HC}$).

The ROI specificity of elevated disparity diverges between the two CVD participants, with V2-dominant disparity in deutan and V1-dominant disparity in protan (Figure~\ref{fig:geometry}B).
Sub-08 showed significantly elevated disparity specifically at V2 (Crawford \& Howell $p = 0.040$), with no significant elevation at V1, V3, or hV4.
Sub-09 showed significantly elevated disparity specifically at V1 ($p = 0.007$), with no significant elevation at V2, V3, or hV4.
Full test statistics and effect sizes ($t$, $d_{cc}$) for these Crawford--Howell comparisons are reported in Supplementary~\ref{app:effect_sizes}.

The $\Delta$RDM heatmaps (Figure~\ref{fig:geometry}A) reveal subject-specific distortion structures.
Sub-08 shows elevated distances among S-cone intermediate pairs at V2, and sub-09 shows a broader reorganization of pairwise distances at V1.
The ROIs of strongest disparity elevation (V1 for sub-09, V2 for sub-08) differ from the ROI used as the distortion-model loss target (hV4, see the \nameref{sec:methods:twocomp} section). Mean pairwise disparity is an existence statement about absolute geometric perturbation in early visual cortex. hV4 is the cortical stage at which the corrective filter must act, so the $\Delta$RDM enters the loss as a geometric convergence check at that ROI. V1/V2 disparity at upstream ROIs is parallel evidence of distortion rather than a competing fit target.

% ── Figure 4 ─────────────────────────────────────────────────────────────────
\begin{figure*}[tb]
  \centering
  \includegraphics[width=\textwidth]{fig3_geometry}
  \caption{\textbf{Each CVD subject shows significantly elevated color representation disparity at a distinct ROI.} \textbf{(A)} $\Delta$RDM $= \text{RDM}_\text{CVD} - \overline{\text{RDM}}_\text{HC}$ in SRM-aligned space at each subject's primary ROI (sub-08 V2, sub-09 V1).
  Warm: CVD distances larger than HC mean; cool: smaller.
  \textbf{(B)} Mean pairwise correlation distance (disparity) per subject per ROI.
  HC band: group mean $\pm 1$ SD ($n = 7$); individual HC LOO as dots.
  Crawford \& Howell tests (sub-08 V2: $p = 0.040^*$; sub-09 V1: $p = 0.007^{**}$, exploratory single-case).
  (A) shows the directional pattern of pairwise deviations. (B) tests the absolute amplitude of pairwise distances against the HC LOO distribution.
  Idiosyncratic ROI specificity (V2 deutan; V1 protan) is inconsistent with a shared group-level gain mechanism.}
  \label{fig:geometry}
\end{figure*}

% ──────────────────────────────────────────────────────────────────────────────
\subsection{The retinal-plus-gain model is structurally insufficient}
\label{sec:results:rc_insufficient}
% ──────────────────────────────────────────────────────────────────────────────

The R+C model is structurally insufficient for both CVD participants.
Fitting this model to each CVD participant yielded ceiling saturation in
both cases.
For sub-08, $\langle 100\%\rangle$ of resample solutions saturated the
grid boundary ($g = \langle 3.0\rangle$).
For sub-09, saturation remained high ($\langle 41\%\rangle$,
$g = \langle 2.95\rangle$).
Boundary saturation at these rates indicates model misspecification,
not a well-constrained optimum \cite{wilson2019}.

A gain $g > 2$ implies that cortical processing reverses the direction of
the retinal shift past the undistorted hue
(Section~\ref{sec:methods:candidates}).
Both participants scored below the Ishihara criterion (sub-08: 5/14 plates
correct; sub-09: 7/14), confirming residual CVD under standard viewing
conditions.
A fitted $g > 2$ is therefore internally inconsistent with confirmed CVD
status: the model claims overcompensation where the participants
demonstrably show impairment.
The saturated boundary is a model failure, not a valid parameter estimate.

The structural cause is a degree-of-freedom deficit.
The function $\delta\theta = (2 - g)\cdot\delta\theta_{\rm Mach}$
displaces hues only along the fixed confusion axis.
No value of $g$ can account for distortions in directions orthogonal to
that axis.
R+C is not considered further as a filter candidate.

% ──────────────────────────────────────────────────────────────────────────────
\subsection{A common cortical model fits both CVD cases}
\label{sec:results:twocomp}
% ──────────────────────────────────────────────────────────────────────────────

The 2-component cortical model fit both participants using the same
functional form.
The deutan and protan cases differ in $(\hat\beta_s, \hat\beta_c)$,
not in model structure.
We estimated $(\beta_s, \beta_c)$ by casting distortion estimation as an
inverse problem (Figure~\ref{fig:pipeline}): a two-parameter forward model is
fit to CVD--HC differences under three loss families, and the fitted transform
is analytically inverted to a stimulus-space correction filter.

% ── Figure 5 — inverse-problem workflow schematic ────────────────────────────
\begin{figure*}[tb]
  \centering
  \includegraphics[width=\textwidth]{fig5_generated_v}
  \caption{\textbf{Inverse-problem workflow for personalised stimulus-space filter design.}
  \textbf{(1)} A two-parameter forward model expresses the per-hue distortion as $\delta\theta(\theta) = \beta_s\cos(\theta - 90^\circ) + \beta_c\cos(\theta - \theta_{\rm conf})$: an S-cone-axis component ($\beta_s$) plus a family-specific confusion-axis rotation ($\beta_c$; $\theta_{\rm conf} = 16^\circ$ protan, $150^\circ$ deutan).
  \textbf{(2)} Three loss families quantify CVD--HC differences --- behavioural JND ratios ($\gamma$), neural representational $\Delta$RDM cosine, and hV4 forward-encoding LOCO; only the LOCO loss couples to the forward encoder.
  \textbf{(3)} A grid search over $(\beta_s, \beta_c)$ minimises the $z$-scored composite to the selected fit $\hat\beta$.
  \textbf{(4)} The fitted forward map is analytically inverted: for each displayed hue $\theta$, the corrected stimulus $\theta_{\rm pre}$ solves $\theta_{\rm pre} + \delta\theta(\theta_{\rm pre}) = \theta$.
  \textbf{(5)} The per-subject filter is $\delta\theta_{\rm filter}(\theta) = \theta_{\rm pre} - \theta$.
  Glyphs are illustrative; the fitted loss landscape is shown in Figure~\ref{fig:landscape}.}
  \label{fig:pipeline}
\end{figure*}

For sub-08 (deutan, $\theta_{\rm conf} = 150^\circ$), the selected
loss combination $\gamma_{\rm OY} + L_{\rm RDM}^{(V2)}$ yielded
$(\hat\beta_s, \hat\beta_c) = (6^\circ, -42^\circ)$.
The held-out test-loss was $\overline{L}_{\rm test} = -2.36$
(IQR $= 2.15$), lower than the alternative candidate
($\overline{L}_{\rm test} = -1.14$; Supplementary~\S S2).
The HC-resample parameter IQR was $(8^\circ, 2^\circ)$.
The strict 7-fold LOO $\hat\beta_c$ range was $[-46^\circ, -38^\circ]$;
all values are negative, confirming a stable sign across held-out
reference sets.

For sub-09 (protan, $\theta_{\rm conf} = 16^\circ$), the selected
loss combination $\gamma_{\rm all} + L_{\rm RDM}^{(V1)}$ yielded
$(\hat\beta_s, \hat\beta_c) = (2^\circ, +24^\circ)$.
The held-out test-loss was $\overline{L}_{\rm test} = -1.54$
(IQR $= 1.42$).
The HC-resample parameter IQR was $(0^\circ, 0^\circ)$, and
$\langle 87.7\%\rangle$ of resamples recovered the same
$45^\circ$ bin.
The strict 7-fold LOO IQR was $(0^\circ, 0^\circ)$.
This position is metric-dependent (Appendix~\ref{app:crossatom}).

In both participants, the dominant distortion component is
$\hat\beta_c$, indicating a primary distortion along each
participant's confusion axis.

The RDM held-out loss on the strict 7-fold leave-one-out was
$\overline{L}_{\rm RDM}^{\rm LOO} = 0.594$ for sub-08 and $0.528$
for sub-09, beating the no-correction baseline on all 7 held-out
folds ($(0,0)$: $1.0$; Supplementary~\S S2).
The production fits rank in the top $5$--$8\%$ of grid combinations,
confirming the improvement is non-trivial relative to the
no-correction floor.
The HC LOO noise ceiling, estimated from split-half HC amplitude
reliability \cite{lagecastellanos2018}, was $0.240$ (sub-08) and
$0.274$ (sub-09); this estimate is conservative because CVD amplitude
noise is excluded.
The production fits recover $52\%$ and $67\%$ of the achievable
range toward the noise ceiling, respectively.

The RDM atom in each subject's winning combination was consistent
with the ROI showing significant geometric disparity:
V2 for sub-08 ($p = \langle 0.040\rangle$) and V1 for sub-09
($p = \langle 0.007\rangle$, Section~\ref{sec:results:geometry}).
The two criteria were independent: the loss combination was selected
by held-out test-loss, and the significant disparity ROI was
identified by Crawford--Howell tests on disparity amplitude.

% ──────────────────────────────────────────────────────────────────────────────
\subsection{Neural data identifies what behavior cannot}
\label{sec:results:neural_role}
% ──────────────────────────────────────────────────────────────────────────────

Fitting behavioral ($\gamma$) and neural (RDM) loss atoms
independently reveals three distinct roles of the neural component.

The neural term captures a protan cortical direction that the
behavioral loss cannot resolve.
Sub-09's behavioral-only fit yielded $\hat\beta_c \approx +4^\circ$
and did not beat the no-correction baseline
($\Delta L = +0.01$, 4/7 folds).
The RDM term captures a signal that the behavioral loss cannot
detect for this participant.

The neural term stabilizes the argmin for sub-08.
The behavioral-only and combined fits share the same argmin
$(6^\circ, -42^\circ)$, confirming that the behavioral atom
independently locates the deutan direction.
Adding the RDM atom reduced the boundary saturation rate from
$23\%$ to $9.3\%$, sharpening the argmin without shifting it.

The neural term also reduces parameter IQR in both participants.
For sub-08, adding the neural term reduced the HC-resample IQR
from $(18^\circ, 6^\circ)$ to $(8^\circ, 2^\circ)$ (PCA) and
$(10^\circ, 4^\circ)$ (SRM).
For sub-09, the IQR collapsed from $(6^\circ, 4^\circ)$ to
$(0^\circ, 0^\circ)$ (PCA, deterministic) and $(0^\circ, 2^\circ)$
(SRM).

The neural-only fit for sub-08 is also non-degenerate
($\hat\beta_c = -26^\circ$; Supplementary~\S S2),
independently corroborating the deutan direction identified by
the behavioral atom.
% ──────────────────────────────────────────────────────────────────────────────
\subsection{Identifiability: mechanism class robust, per-axis value not}
\label{sec:results:identifiability}
% ──────────────────────────────────────────────────────────────────────────────

Parameter recovery fails across all pre-specified identifiability
checks (0/6 significant after FDR correction, BH $\alpha = 0.05$;
Supplementary~\S S3).
The voxel-level parameter-recovery test yielded $f_{10^\circ} < 0.30$
for both production candidates.
The non-dominant axes ($|\hat\beta_s| \leq 6^\circ$ for both
participants) fall below the effective parameter uncertainty of
$\sim\!20^\circ$--$25^\circ$ and are not recoverable
(Supplementary~\S S3).
The dominant axis ($|\hat\beta_c| = 42^\circ$ for sub-08), whose
magnitude exceeds this uncertainty, shows partial recovery under
GT-consistent synthesis (bias $4.7^\circ$; Supplementary~\S S3).

Consistent with the partial recoverability of the dominant axis,
the sign of $\hat\beta_c$ --- the distortion direction --- is stable
across held-out reference sets
(Section~\ref{sec:results:twocomp}).
Sub-08 consistently returns $\hat\beta_c < 0$ across all LOO folds
and resamples.
Sub-09 consistently returns $\hat\beta_c > 0$ under the PCA-basis
loss; sign stability under the SRM-basis loss was not verified
(Supplementary~\S S3).
The distortion direction is recoverable even when its magnitude is not.

The selected fit lies at the global minimum of each participant's composite
loss surface (Figure~\ref{fig:landscape}), but the per-resample argmin
distribution makes the identifiability limit explicit: sub-09's argmin recurs
in $87.7\%$ of resamples yet a secondary basin near $(32^\circ, 0^\circ)$
reflects metric-dependent non-identifiability, and sub-08's argmin disperses
along the $\beta_c$ axis.

% ── Figure 6 — per-subject loss landscape ────────────────────────────────────
\begin{figure*}[tb]
  \centering
  \includegraphics[width=\textwidth]{fig6_landscape}
  \caption{\textbf{Per-subject loss landscape and parameter uncertainty.}
  $z$-scored composite loss over the $(\beta_s, \beta_c)$ grid for each CVD participant's selected loss combination (sub-08: $\gamma_{\rm OY} + L_{\rm RDM}^{(V2)}$; sub-09: $\gamma_{\rm all} + L_{\rm RDM}^{(V1)}$), reconstructed on the full 7-HC pool. Lower (yellow) indicates better fit.
  Red star: selected fit (argmin) --- sub-08 $(\hat\beta_s, \hat\beta_c) = (6^\circ, -42^\circ)$; sub-09 $(2^\circ, +24^\circ)$.
  White points: per-resample argmins over $N = 300$ HC 5-train/2-test resamples, showing parameter uncertainty.
  The selected fits are descriptive low-dimensional embeddings of each participant's distortion, not physiological point estimates (this section).}
  \label{fig:landscape}
\end{figure*}

% ──────────────────────────────────────────────────────────────────────────────
\subsection{Per-subject stimulus-space filter}
\label{sec:results:filter}
% ──────────────────────────────────────────────────────────────────────────────

The per-subject filter $\delta\theta^{\rm filt}_k =
\tilde\theta_k - \theta_k$ is the pre-image of the fitted
2-component transform (Section~\ref{sec:methods:filter}).

For sub-08 (deutan, $\hat\beta_s = 6^\circ$,
$\hat\beta_c = -42^\circ$, $\theta_{\rm conf} = 150^\circ$),
the dominant $\hat\beta_c$ term produces a large-amplitude
correction that varies asymmetrically across hues
(Figure~\ref{fig:filter}).

For sub-09 (protan, $\hat\beta_s = 2^\circ$,
$\hat\beta_c = +24^\circ$, $\theta_{\rm conf} = 16^\circ$),
the smaller $\hat\beta_c$ produces a more targeted correction
concentrated near the protan confusion axis
(Figure~\ref{fig:filter}).

The two correction signatures reflect individual etiologies that
a single retinal-family filter cannot capture.

% ── Figure 7 — per-subject stimulus-space filter (Original | Filtered) ───────
\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\textwidth]{fig7_filter}
  \caption[Per-subject stimulus-space correction filter]{\textbf{Per-subject stimulus-space correction filter.}
  For each CVD participant, the displayed stimulus (\emph{Original}) and the corrected stimulus (\emph{Filtered}) are shown for the eight scanner hues.
  Left block: sub-08 (deutan), $(\hat\beta_s, \hat\beta_c) = (6^\circ, -42^\circ)$.
  Right block: sub-09 (protan), $(\hat\beta_s, \hat\beta_c) = (2^\circ, +24^\circ)$.
  The filtered stimulus is the exact numerical pre-image of the fitted 2-component transform; the per-hue correction $\delta\theta_{\rm filter} = \theta_{\rm pre} - \theta$ is annotated beneath each \emph{Filtered} swatch (8/8 pre-images exact, residual $< 0.01^\circ$ for both subjects).
  Rendering coordinate space: STIM\_LAB CIELab (project convention).
  Quantitative behavioural validation of filter efficacy is deferred to the Phase-3 2AFC arm and will be added once data are collected.}
  \label{fig:filter}
\end{figure*}

% ──────────────────────────────────────────────────────────────────────────────
\subsection{\textcolor{red}{[TODO] Behavioral and neural filter evaluation}}
\label{sec:results:filter_eval}
% ──────────────────────────────────────────────────────────────────────────────

\textcolor{red}{Filter efficacy will be evaluated at two levels. (1)~Behavioral: same-day 2-alternative forced-choice (2AFC) discrimination of pre-specified hue pairs under the per-subject filter versus a no-filter baseline. (2)~Neural: second-session fMRI under the 2-component-derived filter versus a generic Windows accessibility filter (counterbalanced), with LOCO and LORO decoded on second-session data (see the \nameref{sec:methods:behavioral} section). Results to be added once data analysis is complete.}
