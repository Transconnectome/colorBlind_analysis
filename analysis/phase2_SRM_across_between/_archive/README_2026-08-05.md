# phase2_SRM_across_between/_archive — 2026-08-05 확장분

**근거**: `analysis/ARCHIVE_AUDIT_2026-08-05.md` §2.5

가장 중요한 판정 근거: **논문의 symmetric LOSO(`tab:disparity_loso`)와 color-label permutation은 `validation/1B_*`·`1D_*`가 아니라 canonical `rerun_loo_consistent.py`의 자체 출력 JSON 키(`loso_analysis`, `color_label_permutation`)에서 나옵니다.** 즉 1B/1D는 canonical 스크립트에 흡수되어 superseded입니다.

7개 validation 중 **live tex에 보고되는 것은 2C(k-selection) 하나뿐**입니다. 나머지는 `analysis/METHODS_phase2_srm.md`(내부 문서)에만 존재합니다.

| 범주 | 왜 폐기 |
|---|---|
| `superseded_srm_eval/` | `evaluate_srm_*` 세대 및 그 config/summary/viz. `rerun_loo_consistent.py`로 대체 |
| `superseded_validations/` | 1A(HC-only 확인 — 현재 canonical에 하드코딩됨), 1B(LOSO — 흡수됨), 1C(split-half), 2A(ICC — tex hit 0건), 2B(RDM consistency — 죽은 `methods.tex`에만 존재), 2D(alignment 비교), bootstrap CI |
| `validation_1D_permutation/` | 기존 아카이브 확장. 잔여 permutation 스크립트를 형제들과 합류 |
| `visualization_unused/` | 13개 전량. `MAP.md` §(b)가 "SRM `visualize_*` schematics"를 EXCLUDE로 명시. 논문 7개 main figure는 전부 `docs/PAPER/Figures/scripts/`가 생산 |
| `docs_superseded/` | `evaluate_srm_*` 세대의 결과 write-up |

> ⚠ `visualize_scattered_but_parallel.py`는 `MAP.md` E3.3에 이름이 올라 있었습니다(단 "partial stub, no committed output"). 실제 Fig 4A ΔRDM은 `generate_fig3.py` + `srm_precompute/delta_rdm_obs_srm_*.npz`에서 나옵니다. **MAP.md E3.3은 같은 날 수정했습니다.**

**남은 KEEP**: `rerun_loo_consistent.py`(canonical, 원복 금지), `activation_prior_analysis.py`(→ S3), `brain_mapping_utils.py`, `utils/srm_alignment.py`, `validation/2C_optimal_k_selection/`, `validation/aggregate_k_selection.py`, `validation/utils/`, **A3/A4/A5** (`compute_{variance_explained,crossnobis_rdm,pca_cca_replication}.py` — Supplementary 투입 결정, `docs/PAPER/Supplementary/TODO_supplementary_additions.md` S-B), `project_filtered_session.py`(판단보류).

> `brain_mapping_utils.py`와 `utils/srm_alignment.py`는 이번 정리 후 **live importer가 0이 되었습니다** (각각의 importer가 아카이브됨). 공유 유틸 보존 원칙에 따라 남겼으나 실질적으로는 dead code입니다.
