# Deprecated composite-figure generators (2026-05-04)

These 5 scripts produced "single-image" composite PNGs that baked text content into
matplotlib figures (slide1_summary, slide2_activation_decoder, slide3_model_loss,
slide4_status_plans, phase2_meeting_overview).

Superseded by the **3-channel composition** in `presentation/claude_in_ppt_prompts_meeting.md`:
- Channel A (text) is now rendered natively in PowerPoint via Claude-in-PPT
- Channel B (data figures) remains in `presentation/figures/data/` (live scripts in `..` parent dir)
- Channel C (schematics) is generated by AI image models

Kept here for git/history reference. Do not run unless restoring the composite workflow.
If you do restore, update OUT paths to write to `presentation/figures/composites/`
(directory currently does not exist; create with `mkdir`).
